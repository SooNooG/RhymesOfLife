// Password reset pages include this asset in production.
// Keep it present so ManifestStaticFilesStorage can resolve it.
(function () {
  // Intentionally minimal: page-specific behavior is inline in templates.
})();
